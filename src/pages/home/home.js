import React from 'react';
import { Box, Typography } from '@mui/material';
import List from '../../components/list/list';

const Home = () => {
  return (
    <Box sx={{ flexGrow: 1 }}>
      <List/>
    </Box>
  );
};

export default Home;